[project]
name = "shield-normalizer"
version = "0.1.0"
description = "Shield AI — Finding normalization and deduplication"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["normalizer"]